# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*
!.env.example

# Supabase CLI local state — includes a pooler connection string
supabase/.temp

# Stray one-off scripts (should never linger, but just in case)
*.tmp.mjs
*.tmp.ts

# Local tooling cache (impeccable design-review skill) — stores absolute
# local file paths, not project content; never something to version.
.impeccable/
